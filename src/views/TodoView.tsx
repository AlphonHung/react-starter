import React from 'react';
import TodoListSet from '~/components/TodoListSet'

/** 單純用來示範router的view */
const TodoScene = () => (
    <div>
        <TodoListSet />
    </div>
)

export default TodoScene;